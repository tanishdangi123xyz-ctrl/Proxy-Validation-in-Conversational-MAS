# LAPTOP ONLY - analysis and ESP32 serial capture.
#
# src/masenergy/ (the orchestrator) has ZERO dependencies by design and is
# written to Python 3.10 so it runs unmodified on JetPack 6 (3.10) or
# JetPack 7 (3.12). Never add an import here to something in src/.

# ESP32 serial capture
pyserial

# Analysis
numpy
pandas
scipy
statsmodels
matplotlib

# Dataset preparation (laptop-side; ships plain JSON to the Jetson)
datasets

# Model sourcing: scripts/find_model.py (lists candidate native-precision
# GGUF files on HuggingFace) and the one-off hf_hub_download() call that
# actually fetches config.MODEL_FILE at config.MODEL_REVISION into
# models/. This header's "LAPTOP ONLY" label does not quite fit this one:
# unlike every other dependency above, this is needed on whichever machine
# actually issues the download, which in practice has been the Jetson
# itself (it has its own internet access and the model has to live there
# for llama.cpp anyway), not necessarily the laptop. Listed here rather
# than in a Jetson-side file because src/masenergy/ itself must stay at
# zero dependencies per the rule above; this is scripts/-only, never
# imported by anything under src/.
huggingface_hub
